apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ezclaw.fullname" . }}
  labels:
    {{- include "ezclaw.labels" . | nindent 4 }}
data:
  {{- $botfiles := .Files.Glob (printf "botfiles/%s/*" .Values.bot.name) }}
  {{- if $botfiles }}
  {{- range $path, $content := $botfiles }}
  {{ base $path }}: |
    {{- $content | toString | nindent 4 }}
  {{- end }}
  {{- else }}
  config.yaml: |
    {{- .Values.bot.config | nindent 4 }}
  {{- range $name, $content := .Values.bot.files }}
  {{ $name }}: |
    {{- $content | nindent 4 }}
  {{- end }}
  {{- end }}
---
{{- $toolfiles := .Files.Glob (printf "botfiles/%s/tools/*" .Values.bot.name) }}
{{- if or $toolfiles .Values.bot.tools }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ezclaw.fullname" . }}-tools
  labels:
    {{- include "ezclaw.labels" . | nindent 4 }}
data:
  {{- if $toolfiles }}
  {{- range $path, $content := $toolfiles }}
  {{ base $path }}: |
    {{- $content | toString | nindent 4 }}
  {{- end }}
  {{- else }}
  {{- range $name, $content := .Values.bot.tools }}
  {{ $name }}: |
    {{- $content | nindent 4 }}
  {{- end }}
  {{- end }}
{{- end }}
---
{{- $docfiles := .Files.Glob (printf "botfiles/%s/docs/*" .Values.bot.name) }}
{{- if or $docfiles .Values.bot.docs }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ezclaw.fullname" . }}-docs
  labels:
    {{- include "ezclaw.labels" . | nindent 4 }}
data:
  {{- if $docfiles }}
  {{- range $path, $content := $docfiles }}
  {{ base $path }}: |
    {{- $content | toString | nindent 4 }}
  {{- end }}
  {{- else }}
  {{- range $name, $content := .Values.bot.docs }}
  {{ $name }}: |
    {{- $content | nindent 4 }}
  {{- end }}
  {{- end }}
{{- end }}
